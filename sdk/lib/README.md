# SDK Libraries

This directory is reserved for the SDK import libraries and runtime stubs.

Expected layout:

- `libba_texture_sdk.a` / `libba_texture_sdk.so` (Linux)
- `ba_texture_sdk.lib` / `ba_texture_sdk.dll` (Windows)
- `libba_texture_sdk.dylib` (macOS)

The SDK headers live in `../include`.
